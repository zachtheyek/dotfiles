# The --target flag sets the directory to stow from (uses parent directory by default) 
--target=~

# The --ignore flag sets universal patterns (using perl regex) to be ignored by ALL stow operations
# macOS system files
--ignore=\.DS_Store$
--ignore=\.localized$

# Stow's own ignore files (so it doesn't try to stow itself)
--ignore=^/\.stow-global-ignore$
--ignore=^/\.stow-local-ignore$
--ignore=^/\.stowrc$

# Git files 
--ignore=\.git
--ignore=\.gitignore
--ignore=\.gitmodules

# Docs
--ignore=^/README.*
--ignore=^/LICENSE.*
--ignore=^/COPYING

# Editor artifacts
--ignore=Session\.vim
--ignore=\.swp$
--ignore=\.swo$

# Caches/backups
--ignore=^/\.cache
--ignore=\.tmp$
--ignore=\.bak$
--ignore=\.backup$

# Logs/outputs
--ignore=\.log$
--ignore=\.out$

# iTerm configs (sourced directly via iTerm2 preferences. no need to symlink)
--ignore=com\.googlecode\.iterm2\.plist

# Claude local configs
--ignore=\.claude/settings\.local\.json$

# Sanitized example files (these should be committed but not stowed)
--ignore=\.example$

# Chrome directory (excluded from stowing)
--ignore=^/chrome$
