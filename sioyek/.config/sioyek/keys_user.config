# Sioyek Vim-like Keybindings Configuration
# This config provides comprehensive vim-style navigation for mouse-free PDF reading
# All conflicts resolved - no leader key needed (Sioyek doesn't support it natively)

# ========== BASIC MOVEMENT (Vim-style) ==========

# Vertical/horizontal scrolling (hjkl) - PRIMARY MODE
move_down           j
move_up             k
move_left           h
move_right          l

# Arrow keys as alternative
move_down           <down>
move_up             <up>
move_left           <left>
move_right          <right>

# Half-page scrolling (like vim's Ctrl-d/Ctrl-u)
screen_down         <C-d>
screen_up           <C-u>

# Full page scrolling (like vim's Ctrl-f/Ctrl-b)
screen_down         <C-f>
screen_up           <C-b>

# Keep space for convenience
screen_down         <space>
screen_up           <S-<space>>

# Page navigation
next_page           <C-j>
previous_page       <C-k>

# ========== DOCUMENT NAVIGATION ==========

# Go to beginning/end (standard vim)
goto_beginning      gg
goto_end            G

# Go to specific page (using 'go page')
goto_page_with_page_number  gP

# Smart horizontal navigation (like vim's 0 and $)
goto_left_smart     0
goto_right_smart    $

# Top/Middle/Bottom (vim H/M/L)
goto_beginning      H
fit_to_page_width_smart  M
goto_end            L

# Chapter navigation (using gc/gC to avoid conflicts)
next_chapter        gc
prev_chapter        gC

# ========== HISTORY NAVIGATION (Browser-style vim) ==========

# Back/forward in history (like vim's Ctrl-o/Ctrl-i)
prev_state          <C-o>
next_state          <C-i>

# Alternative back/forward
prev_state          <backspace>
prev_state          u

# Redo-like (go forward in history)
next_state          <C-r>

# ========== ZOOM (Vim-inspired) ==========

# Zoom in/out (vi-like + and -)
zoom_in             +
zoom_in             zi
zoom_out            -
zoom_out            zo

# Fit to width (like zz centers in vim)
fit_to_page_width   zw
fit_to_page_width_smart  zW
fit_to_page_width_smart  zz

# Rotate
rotate_clockwise    r
rotate_counterclockwise  R

# ========== SEARCH (Vim-style) ==========

# Search (vim standard)
search              /
search              ?

# Chapter search
chapter_search      c/

# Next/previous search result (vim standard)
next_item           n
previous_item       N

# ========== FILE OPERATIONS (Vim-inspired) ==========

# Open document (like :e in vim)
open_document       o

# Open embedded file picker (like fzf in vim)
open_document_embedded  <C-e>

# Open from current directory
open_document_embedded_from_current_path  E

# Recent documents (like vim's buffer list)
open_prev_doc       O

# Table of contents (like vim's tagbar)
goto_toc            t

# ========== MARKS (Vim standard) ==========

# Set mark (vim standard - m{letter})
set_mark            m

# Goto mark (vim standard - '{letter} or `{letter})
goto_mark           '
goto_mark           `

# ========== BOOKMARKS (Persistent marks) ==========

# Add/delete bookmark (using 'b' namespace since 'leader' doesn't exist)
add_bookmark        b
delete_bookmark     db

# View bookmarks (using 'g' prefix like vim's goto commands)
goto_bookmark       gb
goto_bookmark_g     gB

# ========== HIGHLIGHTS (Vim visual mode inspired) ==========

# Add highlight (using 'a' for annotate since 'h' is taken by movement)
add_highlight       a

# Navigate highlights (using gn/gN to avoid conflicts)
goto_next_highlight  gnh
goto_prev_highlight  gNh

# View highlights (using 'g' prefix)
goto_highlight      gh
goto_highlight_g    gH

# Delete highlight (using 'd' prefix like vim delete operations)
delete_highlight    dh

# ========== VISUAL MARK MOVEMENT ==========
# Note: In visual scroll mode, j/k will automatically control the visual mark
# In normal mode, j/k control document scrolling (see lines 7-8)

move_visual_mark_down   J
move_visual_mark_up     K

# ========== PORTALS (Sioyek's unique feature) ==========

# Portal operations
portal              p
delete_portal       dp
goto_portal         gp
edit_portal         P

# Alternative portal navigation with Tab
goto_portal         <tab>
edit_portal         <S-<tab>>

# ========== LINKS AND SMART JUMP (Vim-inspired) ==========

# Follow link (vim-like 'f' for follow)
open_link           f

# Smart jump (vim-like 'F' for big Follow)
keyboard_smart_jump F

# Visual scroll mode link operations
overview_definition     gl
goto_definition         gd
portal_to_definition    gD

# ========== VISUAL MODE (Vim-inspired) ==========

# Select text (vim visual mode)
keyboard_select     v

# ========== WINDOW/TAB MANAGEMENT (Vim-inspired) ==========

# New window (like vim's :tabnew)
new_window          <C-t>

# Close window (like vim's :q)
close_window        <C-w>

# Quit (vim standard)
quit                ZZ
quit                <C-q>

# Toggle helper window
toggle_window_configuration  <f12>

# ========== MODES AND TOGGLES ==========

# Toggle dark mode (using function keys to avoid conflicts)
toggle_dark_mode    <f8>

# Toggle fullscreen
toggle_fullscreen   <f11>

# Toggle presentation mode
toggle_presentation_mode  <f5>

# Toggle synctex
toggle_synctex      <f4>

# Toggle mouse drag mode
toggle_mouse_drag_mode  <f6>

# Toggle visual scroll mode
toggle_visual_scroll    <f7>

# Toggle highlight links
toggle_highlight    <f1>

# ========== COMMAND LINE (Vim standard) ==========

# Command mode (vim standard)
command             :

# ========== EXTERNAL OPERATIONS ==========

# External search (using 's' for search)
external_search     s

# Copy (standard + vim yank)
copy                y
copy                <C-c>

# ========== QUICK REFERENCE ==========
#
# ESSENTIAL VIM KEYBINDINGS:
# Navigation:
#   h/j/k/l         - left/down/up/right
#   J/K             - move visual mark down/up (in visual scroll mode)
#   gg / G          - beginning/end of document
#   Ctrl-d / Ctrl-u - scroll half page down/up
#   Ctrl-f / Ctrl-b - scroll full page down/up
#   0 / $           - left/right edge (smart)
#   H / M / L       - top/middle/bottom
#   gc / gC         - next/previous chapter
#
# History:
#   Ctrl-o / Ctrl-i - back/forward in history
#   u / Ctrl-r      - undo/redo (via history)
#
# Search:
#   /               - search
#   n / N           - next/previous result
#
# Marks:
#   m{letter}       - set mark
#   '{letter}       - jump to mark
#
# Files:
#   o / O           - open file / recent files
#   t               - table of contents
#   Ctrl-e / E      - file picker / file picker from current dir
#
# Bookmarks (persistent):
#   b               - add bookmark
#   gb / gB         - view bookmarks (current/all)
#   db              - delete bookmark
#
# Highlights:
#   a               - add highlight (then press a-z for color)
#   gnh / gNh       - next/previous highlight
#   gh / gH         - view highlights (current/all)
#   dh              - delete highlight
#
# Portals (cross-references):
#   p               - create portal
#   gp              - goto portal
#   dp              - delete portal
#
# Links:
#   f / F           - follow link / smart jump
#   gd              - goto definition
#
# Zoom:
#   +/- or zi/zo    - zoom in/out
#   zw / zW / zz    - fit to width variants
#
# Visual mode:
#   v               - select text
#
# Toggles (F-keys):
#   F1  - toggle link highlighting
#   F4  - toggle synctex
#   F5  - toggle presentation mode
#   F6  - toggle mouse drag mode
#   F7  - toggle visual scroll mode
#   F8  - toggle dark mode
#   F11 - toggle fullscreen
#
# Misc:
#   :               - command mode
#   y               - copy (yank)
#   s               - external search
#   ZZ              - quit
